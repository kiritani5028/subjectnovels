<div class="offset-md-1 col-md-10 mt-5">
  <ul class="nav nav-tabs nav-justified mb-3">
    <li class="nav-item"><a href="<%= user_path(user) %>" class="nav-link tab-black <%= "active" if current_page?(user_path(@user)) %>">公開した作品　<span class="badge badge-pill badge-dark"><%= @count_posts %></span></a></li>
    <li class="nav-item"><a href="<%= favorites_user_path(user) %>" class="nav-link tab-black <%= "active" if current_page?(favorites_user_path(@user)) %>">お気に入りに追加した作品　<span class="badge badge-pill badge-dark"><%= @count_favorites %></span></a></li>
    <li class="nav-item"><a href="<%= drafts_user_path(user) %>" class="nav-link tab-black <%= "active" if current_page?(drafts_user_path(@user)) %>">非公開の作品　<span class="badge badge-pill badge-dark"><%= @count_drafts %></span></a></li>
  </ul>
  <div class="mt-4">
      <%= render url, novels: user_novels%>
  </div>
</div>